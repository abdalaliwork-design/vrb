# Discord application client ID (public — safe to expose in frontend)
VITE_DISCORD_CLIENT_ID=your_client_id_here

# Backend port (only used in local dev; in production the proxy handles routing)
VITE_BACKEND_PORT=3001

# WebSocket URL override (leave blank to auto-detect)
# VITE_WS_URL=ws://localhost:3001/ws
